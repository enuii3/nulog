<%
  article_updated_at = @article.updated_at.to_date
  article = {
    id: @article.id,
    title: @article.title,
    body: @article.body,
    user_name: @article.user.name,
    updated_at: l(article_updated_at, format: :long),
  }
%>
<%= article.to_json.html_safe %>
